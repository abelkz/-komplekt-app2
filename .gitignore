# Секреты — никогда не коммитим
.env

# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
# pubspec.lock хранится в git: приложение в сторе, и ломающий минор
# в firebase_*, excel, flutter_map или geolocator не должен приезжать
# в сборку сам собой. Обновлять — вручную, workflow «Обновить pubspec.lock».

# IDE
.idea/
.vscode/
*.iml

# Платформенные сборки
android/.gradle/
ios/Pods/
